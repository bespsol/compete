services:
  seq:
    image: datalust/seq:latest
    container_name: compete-seq
    environment:
      ACCEPT_EULA: "Y"
      SEQ_FIRSTRUN_NOAUTHENTICATION: "true"
    ports:
      - "5341:80"
      - "8081:80"
    volumes:
      - seq-data:/data
    restart: unless-stopped

volumes:
  seq-data:
